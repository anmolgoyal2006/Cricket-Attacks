{
  "name": "cricket-clash",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd cricket-clash-cards && next dev --webpack",
    "build": "cd backend && npm run build && cd ../cricket-clash-cards && npx next build",
    "start": "concurrently -n backend,frontend -c blue,green \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd cricket-clash-cards && npx next start",
    "seed": "cd backend && npm run seed"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.15.8"
  }
}
